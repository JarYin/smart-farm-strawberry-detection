# ไลบรารีสำหรับ Raspberry Pi 4 (ใช้รันจริงเท่านั้น ไม่ต้องเทรน)
#
# จงใจไม่ใส่ ultralytics และ PyTorch เพราะกินพื้นที่กว่า 2 GB
# และช้ามากบน Pi เราใช้ tflite-runtime ที่มีขนาดไม่กี่ MB แทน
#
# ติดตั้งด้วย:
#     python3 -m venv --system-site-packages ~/smartfarm-env
#     source ~/smartfarm-env/bin/activate
#     pip install -r requirements-pi.txt
#
# ที่ต้องใช้ --system-site-packages เพราะ picamera2 ติดตั้งผ่าน apt
# ไม่ใช่ผ่าน pip จึงต้องให้ venv มองเห็นแพ็กเกจของระบบด้วย

numpy>=1.24.0
PyYAML>=6.0
opencv-python-headless>=4.8.0   # ไม่มีส่วนแสดงผลหน้าต่าง เบากว่าและติดตั้งง่ายกว่าบน Pi

# ตัวรันโมเดล TFLite — เลือกอย่างใดอย่างหนึ่ง
tflite-runtime>=2.14.0
# ถ้าติดตั้ง tflite-runtime ไม่สำเร็จ (แล้วแต่รุ่นของ Raspberry Pi OS)
# ให้ลองแพ็กเกจนี้แทน:
#   pip install ai-edge-litert
# หรือใช้ TensorFlow เต็มตัว (หนักกว่ามาก ใช้เป็นทางเลือกสุดท้าย):
#   pip install tensorflow

# ควบคุม GPIO — ปกติมีมากับ Raspberry Pi OS อยู่แล้ว
# ถ้าไม่มีให้ติดตั้งผ่าน apt (ไม่ใช่ pip):
#   sudo apt install -y python3-gpiozero python3-picamera2
# gpiozero>=2.0
# RPi.GPIO>=0.7.1

# หมายเหตุ: จงใจไม่ใส่ inference-sdk (Roboflow hosted inference) ไว้ที่นี่
# เพราะเป็นเอนจิ้นคลาวด์ที่พึ่งอินเทอร์เน็ตและมี latency สูง ไม่เหมาะกับควบคุม
# ปั๊มแบบเรียลไทม์บนรถจริง ใช้ tflite-runtime ด้านบนเท่านั้นสำหรับงานนี้
